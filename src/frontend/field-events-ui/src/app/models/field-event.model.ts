export interface FieldEvent {
  id: string;
  externalEventId: string;
  sourceId: string;
  title: string;
  description?: string;
  location: string;
  priority: string;
  status: string;
  occurredAt: string;
  createdAt: string;
}
